#! /bin/bash

input_helpers_confirmation() {
    readonly question="$1"

    read -rp "$question [y/n]? " continue
    continue=${continue:-N}

    case "$continue" in
        [yY][eE][sS]|[yY])
            return 0
            ;;
        *)
            return 1
            ;;
    esac
}
